# 本地归档区

本目录用于存放不参与构建、测试或部署的本地材料，例如：

- `archives/`：旧发布包、旧发布快照和配置备份
- `notes/`：历史审计、交接记录和一次性调研笔记
- `qa/`：截图、运行证据和临时视觉素材
- `design-assets/`：尚未接入代码的原始设计资源
- `private/`：本地敏感材料，仅供当前机器使用

除本说明文件外，目录内容已被 Git 忽略，不要使用 `git add -f` 强行提交。真实 API key 应及时轮换，并只保存在受保护的本地配置中。
