# 历史记录归档

本目录保存已经完成的修复、排查、发布基线和阶段性设计记录，便于追溯决策和验证证据；它们不是当前部署、接口或运营规范。

- `incidents/`：已处理故障、诊断和修复说明。
- `maintenance/`：已完成的目录同步、参数核对和维护记录。
- `decisions/`：已完成的设计决策与实施计划；其中上游价格快照只代表记录日期的结果。
- `releases/`：指定版本的发布交接与上线证据。

需要执行当前操作时，请回到上一级的文档索引，使用当前维护文档；不要直接按历史命令、版本号、线上配置或价格执行。
